# 复制为 .env 后修改密码：cp .env.example .env
CHAT_PASSWORD=change-me-chat
ADMIN_PASSWORD=change-me-admin

# 可选：宿主机访问端口与容器时区。
APP_PORT=8080
CONTAINER_NAME=mini-kb-agent
TZ=Asia/Shanghai

# 可选：覆盖 Docker Hub 镜像或固定版本标签。
MINI_KB_IMAGE=saitomikuya/mini-kb-agent:latest
